import React, { useState } from 'react';
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { MessageCircle, Send } from 'lucide-react';
import { api } from '../../services/api.js';

interface Discussion { id: number; judul: string; isi?: string | null; created_at: string; course: { id: number; judul: string }; user: { nama: string; role: string }; balasan: Array<{ id: number; isi: string; created_at: string; user: { nama: string; role: string } }>; }

export const DiscussionPage: React.FC = () => {
  const queryClient = useQueryClient();
  const [title, setTitle] = useState('');
  const [content, setContent] = useState('');
  const [reply, setReply] = useState<Record<number, string>>({});
  const discussionsQuery = useQuery({ queryKey: ['student', 'discussions'], queryFn: async () => (await api.get<Discussion[]>('/student/discussions')).data });
  const coursesQuery = useQuery({ queryKey: ['student', 'overview'], queryFn: async () => (await api.get<{ courses: Array<{ id: number; title: string }> }>('/student/overview')).data });
  const [courseId, setCourseId] = useState('');
  const createMutation = useMutation({ mutationFn: () => api.post('/student/discussions', { course_id: Number(courseId), judul: title, isi: content }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['student', 'discussions'] }); setTitle(''); setContent(''); }, });
  const replyMutation = useMutation({ mutationFn: ({ topicId, isi }: { topicId: number; isi: string }) => api.post('/student/discussion-replies', { topic_id: topicId, isi }), onSuccess: () => { queryClient.invalidateQueries({ queryKey: ['student', 'discussions'] }); setReply({}); } });
  return <div className="space-y-6 p-4 md:p-6 lg:p-8"><header className="rounded-3xl border border-[#eee9df] bg-white p-6 shadow-sm"><p className="text-xs font-bold uppercase tracking-[0.18em] text-[#615a77]">Forum kursus</p><h1 className="mt-2 text-3xl font-bold text-zinc-900">Diskusi saya</h1><p className="mt-1 text-sm text-stone-500">Tanya dan balas topik dari kursus yang Anda ikuti.</p></header><form onSubmit={(event) => { event.preventDefault(); if (courseId && title.trim() && content.trim()) createMutation.mutate(); }} className="rounded-3xl border border-[#eee9df] bg-white p-5 shadow-sm"><h2 className="font-bold text-zinc-900">Buat topik baru</h2><div className="mt-4 grid gap-3 md:grid-cols-2"><select required value={courseId} onChange={(event) => setCourseId(event.target.value)} className="rounded-xl border border-stone-200 px-3 py-2 text-sm"><option value="">Pilih kursus</option>{coursesQuery.data?.courses.map((course) => <option key={course.id} value={course.id}>{course.title}</option>)}</select><input required value={title} onChange={(event) => setTitle(event.target.value)} placeholder="Judul diskusi" className="rounded-xl border border-stone-200 px-3 py-2 text-sm" /></div><textarea required value={content} onChange={(event) => setContent(event.target.value)} placeholder="Tulis pertanyaan atau pembahasan..." className="mt-3 min-h-24 w-full rounded-xl border border-stone-200 px-3 py-2 text-sm" /><button disabled={createMutation.isPending} className="mt-3 inline-flex items-center gap-2 rounded-full bg-[#1c1d22] px-4 py-2 text-xs font-bold text-white"><Send size={14} />Publikasikan</button></form><section className="space-y-3">{discussionsQuery.data?.map((discussion) => <article key={discussion.id} className="rounded-3xl border border-[#eee9df] bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-wider text-[#615a77]">{discussion.course.judul}</p><h2 className="mt-1 text-lg font-bold text-zinc-900">{discussion.judul}</h2><p className="mt-1 text-xs text-stone-500">{discussion.user.nama} · {new Date(discussion.created_at).toLocaleString('id-ID')}</p></div><MessageCircle className="text-[#615a77]" size={20} /></div><p className="mt-4 text-sm leading-6 text-stone-600">{discussion.isi}</p><div className="mt-4 space-y-2 border-t border-stone-100 pt-3">{discussion.balasan.map((item) => <div key={item.id} className="rounded-2xl bg-[#f6f3ed] p-3"><p className="text-xs font-bold text-zinc-800">{item.user.nama}</p><p className="mt-1 text-xs text-stone-600">{item.isi}</p></div>)}<form onSubmit={(event) => { event.preventDefault(); if (reply[discussion.id]?.trim()) replyMutation.mutate({ topicId: discussion.id, isi: reply[discussion.id] }); }} className="flex gap-2"><input value={reply[discussion.id] || ''} onChange={(event) => setReply({ ...reply, [discussion.id]: event.target.value })} placeholder="Balas diskusi" className="min-w-0 flex-1 rounded-full border border-stone-200 px-3 py-2 text-xs" /><button className="rounded-full bg-[#e4dbfe] p-2 text-[#49435f]" title="Kirim balasan"><Send size={14} /></button></form></div></article>)}</section>{!discussionsQuery.isLoading && !discussionsQuery.data?.length && <div className="rounded-3xl bg-white p-10 text-center text-sm text-stone-500">Belum ada diskusi di kursus Anda.</div>}</div>;
};

export default DiscussionPage;
